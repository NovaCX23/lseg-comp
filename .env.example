# Copy this file to .env and adjust values as needed.
# The app currently reads these as constants in main.py — see MODEL_NAME and DB_PATH.

# Ollama model to use for diagram generation
# MODEL_NAME=qwen2.5-coder:7b

# Path to the SQLite database file (relative to main.py)
# DB_PATH=diagram_history.db
